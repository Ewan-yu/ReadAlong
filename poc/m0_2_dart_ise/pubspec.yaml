name: m0_2_dart_ise
description: M0.2 讯飞 ISE WebSocket Dart 最小验证（HMAC-SHA256 签名 + 分帧上传 + XML 结果解析）
publish_to: none
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  crypto: ^3.0.3
  web_socket_channel: ^3.0.0
  intl: ^0.19.0
  xml: ^6.5.0
